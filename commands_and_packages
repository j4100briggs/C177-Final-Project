FORMAT:
package
  commands
  
  
plotly
  plot_ly (x=0)
MathJax
  helpText ('$$x=at^2+vt+x$$')
  helpText ('$$v=at$$')
  helpText ('$$a=a$$')
library
  library(shiny)
  library(plotly)
  packageVersion('plotly')
  
  building user interface:
  ui
    headerPanel("Title"),
    mainPanel(
      plotlyOutput('plottype', height = "900px")
    )
